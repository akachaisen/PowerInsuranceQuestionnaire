services:
  - type: web
    name: power-insurance-questionnaire
    runtime: node
    buildCommand: npm install
    startCommand: node --experimental-sqlite server.js
    plan: free
    envVars:
      - key: PORT
        value: 3001
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
